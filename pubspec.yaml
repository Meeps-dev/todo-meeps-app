name: my_app
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: 3.9.2

dependencies:
  flutter:
    sdk:
      flutter
      # 👇 Add Firebase core and what you need
  firebase_core: ^3.8.0
  firebase_auth: ^5.3.0
  cloud_firestore: ^5.6.12
  firebase_storage: ^12.4.10

  http: ^1.2.0
  provider: ^6.1.2
  flutter_riverpod: ^2.5.0
  path_provider: ^2.1.4
  sqflite: ^2.3.0
  path: ^1.9.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  cupertino_icons: ^1.0.8
  image_picker: ^0.8.7+5
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
